{
  "name": "wakubo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "oxlint",
    "assets": "node scripts/build-assets.mjs"
  },
  "dependencies": {
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.4",
    "oxlint": "^1.75.0",
    "vite": "^8.2.0"
  },
  "//": "Only `npm run assets` needs these. They pull ~100MB of native binaries, so deploys skip them with --omit=optional (see vercel.json). A plain `npm install` still gets them.",
  "optionalDependencies": {
    "ffmpeg-static": "^5.3.0",
    "sharp": "^0.35.3"
  }
}
