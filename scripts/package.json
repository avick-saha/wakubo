{
  "name": "wakubo-asset-tools",
  "private": true,
  "version": "1.0.0",
  "description": "Image and video tooling for npm run assets. Kept out of the root manifest so deploy builds never install ~100 MB of native binaries they do not use.",
  "dependencies": {
    "ffmpeg-static": "^5.3.0",
    "sharp": "^0.35.3"
  }
}
